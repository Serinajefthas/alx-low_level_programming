#include <stdio.h>

/**
 * main - prints program name
 * @argc: argument count
 * @argv: array of argument strings
 * Return: 0 (success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
