#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * print_binary - prints binary rep of a num
 * @n: num to convert
 */
void print_binary(unsigned long int n)
{
	int i = 0; len = strlen(n);


