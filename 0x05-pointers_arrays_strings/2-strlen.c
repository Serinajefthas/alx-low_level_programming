#include "main.h"
#include <string.h>

/**
 * _strlen - returns length of string in int
 * @s: char string to test
 * Return: int of the string length
 */
int _strlen(char *s)
{
	return (strlen(s));
}
