#include "main.h"
#include <stdio.h>

/**
 * _strcpy - copies str that *src pointer points to,2 buffer *dest pointer points to
 * @dest: pointer that points to str copied to
 * @src: pointer which points to string to copy
 * Return: dest pointer
 */
char *_strcpy(char *dest, char *src)
{

